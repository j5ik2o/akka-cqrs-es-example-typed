apiVersion: v1
kind: Service
metadata:
  name: {{ include "adceet-write-api-server.fullname" . }}
  labels:
    {{- include "adceet-write-api-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "adceet-write-api-server.selectorLabels" . | nindent 4 }}
